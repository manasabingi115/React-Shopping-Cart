App.js
{/* {search === data.name ? (
        <CardsComponent data={data} key={data.id} value={search} />
      ) : (
        <CardsComponent data={data} key={data.id} value={search} />
      )} */}

      {/* {search.includes("nuts") ? (
        <Filters data={data} value={search} />
      ) : (
        <CardsComponent data={data} key={data.id} />
      )} */}

      